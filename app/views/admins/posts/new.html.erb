<h1> New Post </h1>

<%= form_with model: Post.new, url: admins_posts_path do |f| %>
  <%= f.label :title, "Title:" %>
  <%= f.text_field :title %>

  <%= f.label :content, "Content:" %>
  <%= f.text_area :content %>

  <%= f.submit "Create" %>
<% end %>
